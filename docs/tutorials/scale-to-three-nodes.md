# Scale to Three Nodes

1. Bootstrap each node with `vessel init`.
2. Join nodes into the mesh (phase 3+ capability).
3. Deploy with machine selectors in `vessel.toml`.
4. Validate cross-node routing and multi-node log fan-out.

