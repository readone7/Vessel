# How To Add a Machine Behind NAT

1. Ensure outbound SSH works from your workstation to the machine.
2. Run `vessel init`.
3. Configure WireGuard endpoints and fallback relay path (phase 3+).
4. Run `vessel doctor` to confirm transport and mesh health.

